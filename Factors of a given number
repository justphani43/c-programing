#include<stdio.h>
#include<conio.h>
void main()
{
    int i,n;
    printf("enter any number ");
    scanf("%d",&n);
    for(i=2;i<=n;i++)
    {
        if(n%i==0)
        {
            printf(" %d ",i);
        }
    }
}
